import CreateForm from "./CreateForm"

export default function CreateTicket() {
    return(
        <main>
            <h2 className="text-center text-primary">Raise a ticket</h2>
            <CreateForm />
        </main>
    )
}